# Copy this to .env.local for local development
# On Vercel: add these as Environment Variables in the project settings

# ── Django Backend URL ────────────────────────────────────────────────────────
# Set both to your DigitalOcean droplet URL once deployed.
# Example: http://134.122.10.25:8000

# Server-side: used by Next.js API routes (the BFF proxy layer)
DRF_API_BASE_URL=http://localhost:8000

# Client-side: used by authClient.ts for direct browser → Django calls
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000
